---
title: {{TITLE}}
type: entity
tags: [poc, project]
created: {{DATE}}
updated: {{DATE}}
sources: []
refs: []
classification: {{CLASSIFICATION}}
classified-hash: {{CLASSIFIED_HASH}}
---

# {{TITLE}}

<!-- The proof-of-concept project as an entity: the sibling workspace that
     holds the demo built end-to-end on the target role's stack. Links the
     poc-plan doc. -->

## What it is

## Key facts

<!-- Use case; stack plan (link the wiki page of every named technology —
     none ship with the type); purpose (which role-fit gaps it closes; the
     offline-safe demo). -->

## Relations

<!-- The candidate ("author; interview-prep vehicle"); the company ("the
     target whose expected stack the PoC mirrors"); the stack's
     entity/concept pages once created. -->

## Mentioned in

<!-- poc-plan, role-fit-analysis (opaque), the round plans that lead with
     it. -->

## Notes

<!-- Once the PoC has a real runtime, trace one full request round-trip
     end-to-end and record it as a dated note — a ready answer to "walk me
     through what happens when a request comes in". If a real operational
     incident occurs, record root cause, fix, and follow-up correction
     honestly (the second pass, not just the final state). -->
