---
title: {{TITLE}}
type: doc
topic: {{TOPIC}}
tags: [interview, round-plan, strategy, predictions]
created: {{DATE}}
updated: {{DATE}}
sources: []
refs: []
status: {{STATUS}}
classification: {{CLASSIFICATION}}
classified-hash: {{CLASSIFIED_HASH}}
---

# {{TITLE}}

<!-- The plan for one round of the real process: panel, strategy, and the
     predicted question map with answer anchors — the page the
     interview-rehearsal skill draws from and the debrief scores against.
     One file per round, linked from the Rounds table in the topic README.
     Add the round key to tags. Panel research on real people makes this
     C2. Keep the sections that fit the round's format and delete the rest:
     a behavioral/technical round uses segments on a clock; a conversational
     assessment uses axes; a coding + design round uses a coding playbook
     plus design anchors; a leadership round uses assessment axes plus a
     prepared failure retrospective. -->

## Summary

<!-- Round name, duration, format split, what it assesses; one line on the
     panel dynamic (who carries, who observes). -->

## Content

### Panel

<!-- One bullet per panelist, linking their wiki/entities page, with the
     lens they bring to this round. Required: the rehearsal skill plays
     these pages in character. -->

### Lessons carried from previous rounds

<!-- Numbered. A published guide is directional, not literal — expect
     format pivots; translation is tested bidirectionally (recognize a
     technical model hidden in an anecdote); decode metaphor-wrapped
     questions aloud; end technical answers with the outcome they buy;
     honest-state discipline (built vs. tasked, never blurred). Plus any
     framing correction an earlier debrief produced. -->

### Timeline map

<!-- Segment-format rounds only: clock ranges, segment, likely owner. -->

| Clock | Segment | Likely owner |
|---|---|---|

### Strategy and predicted questions by segment / axis

<!-- One subsection per segment or axis. Under each: strategy bullets, then
     predicted questions as **Qn. "..."** with a likelihood in italics and
     an "Anchor:" line — STAR shape with a quantified result for behavioral
     prompts; for technical prompts the named scenario types (explain X to
     a non-technical audience; systematic troubleshooting narrated simplest-
     cause-first with the evidence each step yields); second-seat follow-ups
     from that panelist's lens. Draw ~70% of rehearsal questions from here
     and 30% fresh. -->

### Coding half — playbook

<!-- Coding rounds only: the drill runs against exercises/NNN-*/. In order:
     read the tests first (they are the spec); run the suite and triage by
     root cause; narrate hypothesis -> evidence -> fix -> re-run; fix
     minimally; say aloud if a test looks wrong; re-run after every fix.
     Time budget. Language-agnostic follow-ups if the panel's language
     differs from the round's. -->

### Design half — conversation anchors

<!-- Design rounds only: numbered anchors D1..Dn — requirements first;
     lead with your own worked example; the governance stack (filter at the
     access layer, fail closed, audit, escalate); agentic system design
     (deterministic orchestration around non-deterministic steps, tools as
     the permission boundary, failure containment); evals as
     infrastructure; the full observability answer (golden signals plus
     system-specific signals); integration mechanics (rate limits/backoff,
     idempotency, webhooks vs. polling, provisioning lifecycle,
     silent-failure detection); scaling/tradeoff vocabulary. -->

### The failure retrospective

<!-- Leadership rounds only, prepared word-for-word: real failure, owned
     without hedging -> the specific mechanism that was wrong -> the
     concrete, still-in-use changed behavior, in present tense. -->

### Reverse Q&A

<!-- Prepare 5-6, ask 2-3; strategic first, logistics last (max one);
     split across panelists; framed as an owner, not a recipient. -->

### Watch-outs

<!-- Clock discipline and protecting the reverse Q&A; every story ends in
     a number or an honest magnitude; "I'd have to check" over an
     approximation facing deep expertise; any role-fit gap most exposed in
     this round; anything not to raise unprompted (e.g. a live leveling
     thread). -->

## Notes

<!-- The plan is the prediction; the round-debrief page is the record —
     record format pivots there, not by rewriting this page.
     content-add-notes appends dated entries here: ### {{DATE}} -->
