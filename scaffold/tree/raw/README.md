# raw/ — immutable sources

Drop source material here: articles, papers, PPTX/DOCX exports, transcripts, data
files. Images and binary attachments go in `assets/`.

Rules for agents: read-only. Never modify, rename, or delete anything in this
folder. To bring a source into the content base, run the `content-import` skill.
